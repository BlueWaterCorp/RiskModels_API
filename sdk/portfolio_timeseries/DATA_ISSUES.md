# RiskModels API — Data Issues Digest

_Running list of API / SDK discrepancies found while wiring the 13F portfolio
toolkit. Newest first. Last updated 2026-09-07 (riskmodels-py 0.3.11)._

### Added 2026-09-07 (Pershing — confidential-treatment rows make a filer unstudiable)

- **🔴 `BW-RESTRICTED` confidential-treatment weight is the single best predictor of whether a filer can be studied at all — check it FIRST, before concentration and before spending on a returns fetch.** Pershing Square was run as a third filer precisely because it is the *most concentrated* book available (median effective N 3.3, vs Berkshire's 4.0), on the then-current rule of "pick by concentration". **It failed Stage 0 at 195 bps against a 75 bps stop.** The cause is not concentration and not the method: a large share of the disclosed book is not disclosed. Rows under SEC confidential treatment arrive with `ticker: None` / `security_id: "BW-RESTRICTED"` and have no market data at any price.

  | Filer | median restricted weight | max | Stage 0 | verdict |
  |---|---:|---:|---:|---|
  | Berkshire | **1.2%** | 6.3% | 61 bps | PASS |
  | D. E. Shaw | 16.9% | 44.4% | 78 bps | FAIL |
  | Pershing | **22.7%** | **100.0%** | 195 bps | FAIL |

  Pershing's 2013-12-31 book is **95.6% restricted by weight** (two hidden rows plus one visible 4% position), and it has quarters that are **100% restricted**. Coverage runs from an 83% median down to a **2.6% minimum**. **This is not a tunable threshold** — restricting to the best-covered quarters still fails (104 bps at ≥98% coverage, n=11). The one filer that passes the gate is the one whose book is essentially fully disclosed.
- **🟡 The correlation runs the wrong way.** The concentrated activist managers that look most attractive for a name-level rebuild are exactly the ones who trade through swaps and request confidential treatment. Concentration and studiability are *negatively* related in this sample. **Screen: one `get_filer_holdings` call, compute the share of book weight in rows with no ticker, treat >~10% as unstudiable.** This corrects the selection rule previously recorded in HANDOFF §9.
- **🟢 Pershing has no missing-book problem at all** — 52 of 52 teos back to the 2013 floor return a holdings snapshot with `report_date == teo`, unlike Berkshire's three stub rows. Its 27-ticker union across 13 years also confirms genuinely low name turnover. The book is clean; it is simply half-hidden.

### Added 2026-09-07 (project closeout — terminal-row defect, stub rows, teo reconciliation)

- **🔴 BUG: the TERMINAL row of `get_filer_portfolio` carries a forward return over an OPEN-ENDED window, not one quarter.** Every other row's return is the forward quarter `(teo, teo+1Q]`. The last row's is measured from `teo` to roughly the engine's data horizon. **Test used:** the market layer is ~SPY by construction, so compare each row's `portfolio_market_return` against SPY over that row's own forward quarter. Non-terminal rows agree to ~60 bps median; terminal rows deviate **2.7×–34×** that, and the horizon that would reproduce them is never one quarter:

  | filer | terminal teo | dev vs SPY | × median | implied horizon |
  |---|---|---:|---:|---:|
  | Berkshire | 2025-12-31 | +1242 bps | 21× | 1.9 quarters |
  | Greenlight | 2023-12-31 | +4146 bps | 34× | **9.2 quarters** |
  | Appaloosa | 2026-03-31 | +790 bps | 7× | 0.7 quarters |
  | D. E. Shaw | 2026-03-31 | +262 bps | 5× | 0.7 quarters |
  | Pershing | 2026-03-31 | +130 bps | 3× | 0.7 quarters |

  Greenlight is decisive: its last filing is 2.5 years stale and its "one quarter" return matches SPY over **9.2 quarters** — consistent with all terminal rows ending at the same calendar horizon regardless of their teo. **Consequence: drop the terminal row from any quarter-aligned analysis.** Including Berkshire's pushed its Stage 0 gate from **61 bps (PASS) to 84 bps (STOP)** on the strength of one row, and it contaminated the endpoint-based 46-quarter layer Sharpes. **Worth raising with the API team** — we can detect and drop it, but not fix it upstream.
- **🔴 The three Berkshire "missing-book" quarters are STUB ROWS, not missing books.** 2014-09-30, 2020-09-30 and 2025-03-31 carry a `portfolio_gross_return` but no holdings snapshot — because the rows describe a portfolio of **one or two names**: `n_holdings_active` 1/1/2 and `total_aum_usd` $620M / $386M / **$885K**, against a **$73.4B** median for the 42 real quarters (0.845% / 0.526% / **0.001%**). `get_filer_holdings(as_of=X)` correctly falls back to the prior quarter's ORIGINAL because there is no full book behind the row. **Not recoverable by any `as_of` escalation or `amendment_type` filter, and not worth recovering.** Their drop-idio returns average **837 bps/q vs 372** for real quarters — which is the entire A→B step of the Sharpe bridge, so the 46-quarter 1.003 is *contaminated*, not merely sample-selected.
- **🟡 CORRECTION to the 2026-07-27 "teo series has calendar gaps" note.** Full Berkshire reconciliation: `get_filer_portfolio` returns **50 rows**; **46** carry a `portfolio_gross_return`; **43** have a matching holdings book; minus the terminal row = **42 usable**. Of the four teos previously called "missing" (2015-06-30, 2021-06-30, 2023-09-30, 2023-12-31), only **2021-06-30** is genuinely absent from the series — the other three are **present as rows but carry a null `portfolio_gross_return`**, as does 2013-06-30. The practical advice is unchanged (use `teo + 3 months`, never "the next teo in the series"), but the characterisation was imprecise.
- **🟢 Lesson generalised (second time).** Both bugs found in this project — NaN poisoning and the terminal row — were cases where a *plausible-looking* value flowed through a pipeline that never checked it against an independent quantity. The cheap defence in both cases was the same: compare against something the model already implies (coverage-vs-value for NaN; the market layer vs SPY for the terminal row). **Build that cross-check into any new endpoint consumer.**

### Added 2026-08-24 (RRG regime classifier port — zarr factor plane, PIT labels, subsector coverage)

- **🟢 The FFX synthetic-factor return plane IS reachable from our environment** — `gs://rm_api_public/eodhd/ds_synth_factors.zarr` opens with `storage_options={"token":"anon"}` once `gcsfs`+`zarr` are pip-installed (they were missing; installed). 106 factor columns, **daily returns 2000-01-04 → 2026-07-02**. Aman's 19 (11 sector ETFs + 8 subsector codes) all present; the other subsectors have missing data (why he hard-codes 8). **Note the plane ends 2026-07-02** (the constituents-CSV date) — fine for our study (report dates ≤ 2026-03-31) but labels can't run past early July 2026 without a refresh.
- **🟢 Aman's RRG port reproduces his result (validation gate PASS).** His in-sample test (entry into Improving, sectors, 24-week) = 517 signals / 76.60% prob-gain / 6.15% mean; our port = **515 / 76.12% / 6.07%**. 2-signal gap is data (SPY endpoint + plane end date), not method. Method (ewm span=10, thresholds=100, quadrant rules) copied verbatim.
- **🟢 Point-in-time labels verified causal (no leakage).** `label_fn(as_of)` truncates the weekly series at `as_of` and recomputes; matches the full-history label at that week exactly across 8 dates 2001–2026 (19 factors each). RRG is cumprod + causal EWM, so truncation reproduces — the momentum/reversion answer is not contaminated.
- **🔴 Subsector regime coverage is ~10% of D.E.Shaw book weight — unusable.** Only 8 subsector factors are in Aman's universe, and D.E.Shaw's book maps just **9.9% median weight** (min 8%) to them — far below a ~40% usability bar. **Sector-level (11 factors, 91% coverage) is the only usable regime split.** State this on any regime result; do not quote subsector quadrant numbers.
- **🟡 The regime split decomposes a mostly-beta signal — quadrant differences may be sector beta, not selection.** The +1..10 window is levered market beta (α≈0, β=1.29; see report-date study §2). Sector quadrants differ in beta, so per-quadrant return differences (e.g. Leading underperforming Weakening post-report, t=−2.73 on the largest-vs-smallest-weight pair) may reflect sector beta in those specific windows. Characterisation of positioning, not an alpha source. Next step to disentangle: regress each quadrant basket on SPY.

### Added 2026-08-17 (NaN-poisoning bug in the gross-return path — affects D.E.Shaw AND Berkshire)

- **🔴 BUG (fixed): `build_lagged.window_return` compounded `∏(1+r)` WITHOUT dropping NaN return days, so one bad name nulled an entire quarter.** A name with a single NaN return day in the window returns NaN (not None), which still counted toward `covered` in `portfolio_window_return` but poisoned the weighted sum → the whole portfolio window went NaN. Smoking gun: D.E.Shaw 2013-09-30, ticker `Z` (Zillow, freshly IPO'd, all 10 window-days NaN), **weight 0.03%**, nulled the entire quarter. The decomposition path (`name_layer_windows`) already dropped NaN days; the gross path did not — an inconsistency. **Fix:** drop NaN days before compounding, skip all-NaN names (→ None); plus a defensive `isnan(R)` guard in `portfolio_window_return`.
- **⚠️ This bug SILENTLY DROPPED QUARTERS and moved published results materially — the exact "dropped-for-a-data-reason" trap Conrad flagged on the Berkshire Sharpe.**
  - **D.E.Shaw report-date study:** dropped **13 of 49** quarters (all 2013–2017, the weak early half). Fixed → n=49. The headline **flipped**: post-report-vs-control paired t **2.86 → 1.15 (significance lost; 1.22 at 82% cov, 1.15 at full 93%)**; the effect is now beta (regression α t=−0.64, β=1.29), not special. The buggy 36-quarter sample had isolated the strong recent years.
  - **Berkshire lagged backtest (prior, already-audited deliverable):** the same shared `window_return` dropped **7 windows** — Stage 1 n **35 → 42**, lagged gross Sharpe **0.85 → 0.99**, mean 415.8 → 453.6 bps/q. **The prior Berkshire numbers were DEFLATED by this bug.** This needed a dedicated Berkshire re-run + re-audit (LAGGED_RESULTS.md, attribution_audit.xlsx) — since completed; current numbers are in FINDINGS.md. **NOT silently rewritten here** — flagged for review because it reopens presented figures.
- **🟢 Lesson generalised:** any pipeline that renormalises to "covered" weight must treat NaN the same as missing. Coverage looking healthy (e.g. 68%) is not proof the window computed — a NaN name inflates `covered` while nulling the return. Always check the returned value, not just the coverage.

### Added 2026-08-17 (D.E.Shaw report-date window study — history floors, coverage, layer validation)

- **🟡 The studiable history is capped by the DAILY-RETURNS floor (2013-07-29), NOT by holdings or filings.** D.E.Shaw's `get_filer_portfolio` series runs **81 quarters back to 2005-12-31**, and holdings snapshots with `report_date == teo` exist back to **2013-09-30** (all 31 older books 2013–2021 are present, report_date matches, zero missing-book gaps — the only DEShaw missing-book quarters are recent: 2023-12-31, 2024-03-31). But `get_ticker_returns` has no data before **2013-07-29** and `get_returns_decomposition` none before **2011-07-27**, so any post-report-window measurement is floored at 2013. **Net: ~13 studiable years (49 quarters, 2013-09-30 → 2026-03-31), not the 20 requested.** The binding constraint is returns, so no amount of holdings/portfolio history helps.
- **⚠️ D.E.Shaw layer decomposition FAILS validation again — the idiosyncratic layer will not reproduce.** Reproducing the endpoint's own unlagged quarterly layer returns for D.E.Shaw (covered-name renormalised): market mean|diff| ~50bps (median 28), sector ~24 (17), subsector ~12 (10), **idiosyncratic ~102 (median 73) — well over the 50bps bar.** Same failure mode as the lagged-backtest Stage 0 (79bps): high turnover (~22%/q) + thin coverage means the name-level residual (the arithmetic remainder, hence the most coverage-sensitive layer) can't be rebuilt from the covered subset. **Consequence: no D.E.Shaw factor split (market vs sector vs selection) can be published from a partial-universe rebuild.** The market layer is least coverage-sensitive and roughly reproduces; the idio layer is the one that breaks. This is a *coverage* limitation, not an endpoint bug — the decomposition is exact per name; we just can't cover enough of a 2000-name book.
- **🟡 Coverage on D.E.Shaw is structurally ~80% of book weight and can't easily be raised.** The book is ~2000 names (1000-row API cap; ~90% of weight in ~550 names), and the illiquid tail is **intentionally excluded from the ERM3 3000 universe** (confirmed: by design, not a data error), so those names have no returns/decomposition at any price. Extending the cache to the top-92%-weight union across all 49 studiable books required fetching ~767 additional names (returns+decomp, ~$46) and lifts median covered weight from ~82% to **~93%** (min 75%). Even at 93% the idiosyncratic layer still fails validation (median 59bps) — the residual is too coverage-sensitive to rebuild. Every windowed result must renormalise to the covered book and state coverage.
- **🟢 Report-date window primitive (trading-day offsets after `teo`) works cleanly on cached data.** Anchor = last trading day ≤ report_date; windows [+d_start..+d_end] in trading days map to `(start, end]` date bounds fed to the existing `portfolio_window_return` / `portfolio_window_layers` helpers. Quarter-ends are often non-trading days (e.g. 2023-09-30 Sat) — anchoring to the last trading day ≤ teo handles this without special-casing.

### Added 2026-08-03 (audit session — amendment fields, synthetic filing dates)

- **🟡 Filing dates look CONSTRUCTED, not observed — the "45-day lag" is largely a default.** `filing_date − teo` is **exactly 45 days for 63–68% of periods across all five filers** (Berkshire 63%, Pershing 63%, Appaloosa 68%, Greenlight 67%, D.E.Shaw 65%); the remainder are almost all 46–48 (a report_date+45-day figure rolled to the next business day). Four *independent* managers do not file exactly on the deadline 65% of the time — this is a computed `report_date + 45 business-day` placeholder, not measured filing timestamps. **Consequence: "45 days is real" overstates it — the median-45 result measures the §13(f) deadline convention baked into the data, not observed filing behaviour.** The ~35% "other" values (and the amendment tail) are the only genuinely observed variation. Separately, `search_filers` metadata exposes `filing_date_source: "placeholder_lag_75d"` — a *different, cruder* +75-day placeholder (report+75; e.g. Berkshire 2025-12-31 → 2026-03-16) that we do NOT use; our portfolio/holdings dates use the +45 convention. **Neither the portfolio nor the holdings response carries a `filing_date_source` flag** — only `search_filers` does, so we cannot tell per-row which dates are observed vs defaulted.
- **🟢 Amendment fields exist and are historical (corrects the "no form_type" note).** `get_filer_holdings` envelope carries `filing_type` ("13F-HR"), `amendment_type` ("ORIGINAL"), `accession_number` on both current and historical vintages. `get_filer_holdings(as_of=X)` resolves to the ORIGINAL book on/before X, so its `filing_date` is the clean original — unlike the portfolio endpoint's `filing_date`, which is amendment-contaminated for a minority of teos. Filtering to ORIGINAL removes the >90d lag tail (Berkshire max 228→~48).
- **⚠️ The 3 "missing-book" quarters do NOT resolve via the amendment filter.** For Berkshire 2014-09-30 / 2020-09-30 / 2025-03-31, no ORIGINAL (or any) holdings snapshot with report_date == teo exists in the store — `as_of` returns the prior quarter's ORIGINAL. This is a genuine holdings-coverage gap, independent of amendments. D.E.Shaw has the same pattern (e.g. no 2023-09-30 book).

### Added 2026-07-27 (lagged-backtest build — daily name-level rebuild)

- **🔴 Some `teo`s have a `portfolio_gross_return` but NO holdings snapshot.** For Berkshire, `get_filer_holdings(as_of=X)` never returns report_date == teo for **2014-09-30, 2020-09-30, 2025-03-31** — even at as_of = teo+120d it returns the *prior* quarter's book. Yet `get_filer_portfolio` carries a gross return for those teos. So the two endpoints disagree on which quarters exist. These quarters cannot be rebuilt from holdings; a daily name-level backtest must detect (report_date != teo) and exclude or carry the prior book. 3 of 46 for Berkshire.
- **🔴 The `get_filer_portfolio` `teo` series has calendar gaps.** Berkshire is missing **2015-06-30, 2021-06-30, 2023-09-30, 2023-12-31** entirely. A backtest that sets each window's end to "the next teo in the series" silently doubles those windows (produced a −600 bps rebuild artifact). The forward return is one *calendar* quarter — use `teo + 3 months`, not the next available teo.
- **🟢 CORRECTION (supersedes an earlier wrong claim): the API DOES provide a per-day, per-name additive factor decomposition.** `get_returns_decomposition(ticker, years=15)` (`GET /returns-decomposition`) returns per trading day: `gross_return`, `l1_factor_return` (**market**), `l2_factor_return` (**sector**, incremental/net of market), `l3_factor_return` (**subsector**, incremental/net of sector), `l3_residual_return` (**idiosyncratic**), plus the `lN_combined_factor_return` cumulatives. The four map exactly to the orthogonal ERM3 layers and **sum to `gross_return` daily to ~5e-10** (rare single-day blips to ~2.4e-3); same model as the holdings `decompose` (`factor_set_id=SPY_uni_mc_3000`, v3.0), point-in-time to 15y. Separately, `get_ticker_returns` carries per-day L3 hedge ratios and explained-risk fractions in columns we had been dropping. **So the point-in-time / lagged layer decomposition IS buildable — the earlier note that it was "current-date only" and "blocks the sector+subsector / drop-idio question" was wrong.** The ER-share method failed (±440–530 bps) only because ER are variance shares, not return weights; `get_returns_decomposition` is the correct source. Cost ~$0.02/call.
- **🟡 `portfolio_gross_return` appears to be ERM3 model-implied, not realized price return.** A buy-and-hold rebuild from actual `get_ticker_returns` runs median +12 / mean +63 bps/q **above** the endpoint over the same windows and books (36 Berkshire quarters, mean |diff| 65 bps, median 46). The tail (high-vol quarters) drives the mean; typical quarters agree to ~12 bps. Daily-rebalanced vs buy-and-hold aggregation is NOT the cause (both within 4 bps). Consistent with the endpoint returning a factor-model gross rather than realized P&L.
- **🟡 GOTCHA: `get_returns_decomposition` series contain occasional NaN days** (the same `L3 sum = nan` data-quality flag `get_ticker_returns` warns about). Naive `prod(1 + layer_daily)` over a window hits one NaN and returns NaN for the whole name, which then poisons the portfolio sum (a single bad name → NaN for the entire quarter's layer). Drop NaN days before compounding. Bit us once — 19 of 42 Berkshire windows silently went NaN until fixed.
- **🟡 `get_filer_holdings(as_of=teo+55d)` is too tight for late-filed books** — returns the prior quarter's book when the filing landed >55d after quarter-end. Escalate the buffer (teo+90, teo+120) and verify `report_date == teo`; cap below teo+~136d so you never overshoot into the next quarter's filing.

### Added 2026-07-26 (revision pass — lag dedup, Sharpe, orthogonality, staleness)

- **🔴 `filing_date` on portfolio rows is NOT reliably the ORIGINAL 13F date — for some periods it is a 13F-A amendment date.** `get_filer_portfolio` returns exactly one row per unique `teo` (Berkshire 50 rows = 50 unique teos; 0 duplicates for all four filers), so there is no dedup to do — but the single `filing_date` recorded for a minority of teos is a later amendment, not the first public disclosure. Smoking gun: Berkshire's `teo` 2023-09-30 AND 2023-12-31 both carry `filing_date` 2024-05-15 (one amendment date stamped on two periods → 228d and 136d apparent lags). This inflates the lag maxima (Berkshire 228, Pershing 168, Greenlight 114). **The median lag (45d) is clean; the tail is amendment contamination.** ~~No `form_type` field is exposed~~ **CORRECTED 2026-08-03: `filing_type` ("13F-HR") and `amendment_type` ("ORIGINAL"/"AMENDMENT") ARE exposed on the `get_filer_holdings` envelope (plus `accession_number`), and are populated on HISTORICAL vintages, not just the current one.** We were looking for the wrong field name (`form_type`). Filtering to `amendment_type == "ORIGINAL"` strips the amendment tail (see the 2026-08-03 block below).
- **Deduped lag (min filing_date per teo, full history):** Berkshire n=50 median=45 (45–228), Pershing n=80 median=45 (23–168), Appaloosa n=40 median=45 (41–48), Greenlight n=73 median=45 (44–114). No time drift (|corr|≤0.23). Every filer files at the §13(f) wire. *(Re-verified 2026-09-07 against the cached portfolio rows, counting every row carrying a `filing_date`: Berkshire **50**, Pershing **81**, Appaloosa **41**, Greenlight **73**, D. E. Shaw **81**; medians all 45; maxima 228 / 168 / 48 / 114 / **319**. Pershing and Appaloosa are one row higher than recorded above — the earlier counts were taken over rows carrying a gross return rather than all rows. Percentages exactly-45: 64 / 63 / 68 / 67 / 65. Use these figures.)*
- **Pershing has one genuine sub-45 outlier: `teo` 2020-09-30, filing 2020-10-23 (23d).** The other two sub-45 rows are 44d (at-deadline originals). The 23d row is either a real early original or a mis-mapped amendment — now resolvable via the holdings-envelope `amendment_type` (see 2026-08-03 block).
- **🟢 ERM3 layers are orthogonalized (hierarchical L1→L2→L3).** `decompose()` ER (explained-variance) shares partition to exactly 1.0 (`_data_health.er_sum` = 1.0000000; market+sector+subsector+residual = 1.0), and hedge ratios re-estimate at each level (sector net of market at L2, subsector net of sector at L3). So `portfolio_sector_return` is the sector tilt **net of** market — the tradeable quantity, not a raw sector exposure. `factor_set_id='SPY_uni_mc_3000'`, `model_version='3.0'`.
- **Layer→gross closure error (linking residual) is real but small per quarter.** `gross − Σ(market,sector,subsector,idio)`: mean +4.5 bps/q, **median |residual| 33 bps/q**, max |residual| 214 bps. It exceeds that quarter's |idio contribution| only **7% of quarters** (idio's own quarterly magnitude is ~249 bps median). But median |residual| (33 bps) equals idio's *mean* contribution (33 bps/q) — so the average idio sleeve sits inside the decomposition's own closure noise. `identity_residual` field is present but null.
- **Sanity check — market-layer Sharpe ≈ SPY.** Berkshire market-layer annualized Sharpe 0.84 vs SPY daily Sharpe 0.82 over 2013-09→2025-12 (forward-quarter SPY Sharpe 0.98). The decomposition's market component reproduces the index — a green light on the attribution engine.
- **Staleness channel is second-order over the 45-day gap.** Berkshire portfolio-level ER-loading drift across recent disclosed vintages: mean |Δ sector ER-load| ≈ 137 bps/q (⇒ ~69 bps over 45d) and |Δ subsector| ≈ 7 bps/q (⇒ ~3 bps). A lag-aware backtest can hold disclosed weights fixed across the reporting gap with negligible error — the correction is about entry *timing*, not book staleness. (Caveat: `l3_*_er` on holdings rows may be current-model, so this isolates the weight/turnover channel.)

### Added 2026-07-26 (reporting-lag / layer-attribution diagnosis)

- **🔴 `available_date` does NOT exist at all** (CORRECTED 2026-08-03). Earlier this entry said the holdings envelope carries a *null* `available_date` key — **that was wrong; there is no `available_date` or `availability_date` key on the portfolio rows or the holdings envelope.** Per the schema owner: `filing_date` is the canonical knowledge-time field for 13F; the `availability_date` spelling is used only on ETF/benchmark endpoints. `get_filer_portfolio` rows carry `teo` + `filing_date`; the `get_filer_holdings` envelope carries `report_date`, `filing_date`, `as_of_basis='filing_date'`, `filing_type`, `amendment_type`, `accession_number`. Use `filing_date` as the availability field; do not look for `available_date`.
- **⚠️ Per-quarter returns are aligned to `teo` (report date), NOT `filing_date`.** `portfolio_gross_return` at T is the forward return over (T, T+1Q], **starting at quarter-end**. The ~45-day reporting delay is therefore **not baked in** — the series implicitly assumes you hold the book from quarter-end, ~45 days before it is public. Any "point-in-time" claim on this series has a half-quarter of look-ahead per period.
- **Lag `filing_date − teo` is a hard ~45 days for all four filers.** Median = mode = 45 (the §13(f) deadline); no time drift (|corr with time| ≤ 0.23). Ranges: Berkshire 45–228 (n=50), Pershing 23–168 (n=80), Appaloosa 41–48 (n=40), Greenlight 44–114 (n=73). Outliers >90d are amendments; managers file at the wire. Pershing's min 23 (some early/amended filings) is the only sub-45 cluster.
- **Partial-quarter (filing_date → next teo) returns are NOT exposed.** The endpoint returns whole-quarter forward returns only; the (teo, filing_date] sub-window can't be subtracted. A lag-aware series entering at `filing_date` therefore cannot be built from `get_filer_portfolio` alone — it needs a daily name-level rebuild (`get_filer_holdings(as_of=filing_date)` × `get_ticker_returns`, chained from filing_date). A whole-quarter-lag proxy is cheap but measures a different question (re-attributes whole quarters; moves Berkshire layers <1 bp under low turnover).
- **Layer additivity is approximate, not exact.** `gross − Σ(market, sector, subsector, idio)` has mean +4.5 bps/q but median |residual| 33 bps and max |residual| 214 bps (`identity_residual` field is null). The means reconcile to gross within ~5 bps/q; per-quarter the layers miss by a signed ~33 bps that cancels. Treat the decomposition as a diagnostic attribution, not an identity.

### Added 2026-07-19 (Week-2 deliverables session)

- **🟢 CAPABILITY (good news): `get_filer_portfolio` already returns point-in-time factor-decomposed returns.** Each period row (`teo`, quarterly) carries `portfolio_gross_return` split into `portfolio_market_return` / `portfolio_sector_return` / `portfolio_subsector_return` / `portfolio_idiosyncratic_return`, computed from **that quarter's actual holdings**. This means a correct point-in-time overlay backtest needs **no per-vintage decompose fan-out** — the engine has already done the per-quarter attribution. This is what powers `charts/full_history_backtest.png`.
- **⚠️ GOTCHA on the above: those returns are FORWARD, not trailing.** The return at `teo` T is the return of the book *known as of T* over the **next** quarter (T, T+1Q]. Verified: aligning `portfolio_market_return` to SPY over the *following* quarter → corr ≈ +0.74, raw β ≈ +0.94 (Berkshire); aligning to the *trailing* quarter → a nonsensical **negative** β. Anyone regressing these against a benchmark must forward-align or they'll get garbage (this bit me on the first D4 run). Worth documenting in the field's schema.
- **`get_ticker_returns` supports deep history (`years=13`), floor 2013-07-29.** SPY and AAPL both return 3,267 daily rows back to **2013-07-29**. So daily-return backtests can't go before mid-2013 even though portfolio-level history reaches 2005 for some filers (Pershing, Greenlight). *(Corrected 2026-09-07: this entry originally said 2013-07-22. Re-verified against the cache — SPY, AAPL and KO all start 2013-07-29 with 3,267 rows. The row count was right, the date was not. **2013-07-29 is the floor**, matching DESHAW_REPORT_DATE §0.)*
- **`as_of` returns genuinely distinct historical vintages** (confirms bi-temporal is real, not just latest). Berkshire `as_of` 2015-06-30 → book with IBM/WMT/PG; 2018-06-30 → book with DAL; 2021-06-30 → book with GM. Each is a different quarter's holdings.
- **`get_ticker_returns` emits a `ValidationWarning` for some tickers:** _"L3 explained-risk components sum to nan, expected 1.0 ± 0.05."_ Benign (a data-quality flag on the L3 decomposition metadata attached to the returns response), does not affect the returns series itself, but noisy in logs.
- **The market-neutral overlay is BOTTOM-UP (per held stock), so it does not hedge sectors the book omits entirely.** Berkshire holds ~0% Health Care (SPY ~11%), yet the overlay shows **$0** in health-care ETFs because no held name references them — the overlay only shorts/longs ETFs that appear in the held stocks' `decompose().hedge` dicts. Not a bug, but a design property worth a decision: bottom-up per-position hedge vs. a top-down index-relative hedge (which *would* short/long omitted sectors). See `charts/long_etf_explainer.png`.


- **🔴 `adj_mv` / `total_aum_usd` units are inconsistent between the latest period and historical vintages.** Berkshire 2025-12-31: `sum(adj_mv)` = 176,510,884 and `total_aum_usd` = 176,510,883.98 (~$176M — 1000× too small for Berkshire; looks like thousands). Berkshire 2020-03-31: `sum(adj_mv)` = 116,493,313,000 and `total_aum_usd` = 116,512,036,000 (~$116B — raw dollars, correct). So the current period appears to be in **thousands** while historical vintages are in **raw dollars**. NOTE: on 2026-07-07 the current period read 176,510,883,983 (raw $ = $176.5B); it now reads 176,510,884 — the scale flipped by 1000× in the last week. Weights/leverage are unaffected (scale-invariant), but any absolute-dollar consumer breaks. **Highest-priority fix.**
- **🔴 The `adj_mv` scale bug is ALSO cross-filer, not just cross-period.** Same report date 2025-12-31: Appaloosa `sum(adj_mv)` = 5,495,562,638 (~$5.5B, raw dollars, plausible) while Berkshire reads ~$176M and Pershing ~$11.5M (both 1000× compressed). So the unit varies **by filer within the same quarter** — there's no single multiplier to correct it downstream. Suggests the scale is inherited from each filer's raw 13F (some file in thousands, some in dollars) and isn't normalized in the pipeline.
- **`search_filers` does not match a zero-padded CIK string** (`q="0001067983"` → 0 results), and a raw-number/name query returns fuzzy hits that can resolve to a *different* CIK. Reliable resolution is the deterministic `BW-FILER-CIK{cik.zfill(10)}` id confirmed via `get_filer`, or a name search with a name-token check.
- **Two of the four seed-filer CIKs from the brief don't exist as filer ids.** Appaloosa `0001006438` → `APIError: Filer not found` (working id is `0001656456`, "Appaloosa LP"). Greenlight `0001522887` AND `0001080124` → both "Filer not found"; the real entity resolves by name search to `0001079114` ("GREENLIGHT CAPITAL INC"). Worth confirming the canonical CIK→filer mapping.
- **Greenlight's latest available filing is 2023-12-31** — ~2 years stale vs the others' 2025-12-31. And its largest disclosed "position" (27.5% of book) is a confidential-treatment `BW-RESTRICTED` row, with another unresolved-FIGI row below — so both its concentration read and any overlay for it are unreliable.
- **Holdings history floor is much shallower than portfolio history, and the gap varies wildly by filer.** Berkshire: `get_filer_portfolio` floor 2013-06-30 vs `get_filer_holdings` floor 2013-09-30 (1 quarter). Pershing (CIK 0001336528): `get_filer_portfolio` floor **2005-12-31** (80 periods) vs `get_filer_holdings` floor **2013-06-30** — a ~7.5-year gap. So the "holdings = portfolio + 1 quarter" pattern does NOT generalize; per-position history is unavailable for years that portfolio-level aggregates cover.
- **Neither endpoint reaches "2006→present" uniformly.** Berkshire portfolio starts 2013-06-30; Pershing portfolio starts 2005-12-31. Holdings for both start 2013. The blanket "history back to 2006" claim doesn't hold per-filer.
- **`get_filer_holdings` below the holdings floor raises `APIError` ("No holdings were known for this filer as of the requested date"), not an empty list.** Callers must catch it rather than test for `[]`.
- **`filing_date` is null on the full `get_filer_holdings` fetch** (no `as_of`, no `limit`), but populated when `limit=5` or when `as_of=` is passed. Inconsistent — forces a fallback lookup via the portfolio series.
- **`decompose` exposes no size beta, value beta, or stock-specific Sharpe** — only `style.explained_variance` and `stock_specific.explained_variance` (both `hedgeable:false`, style `role:diagnostic`). The "four-factor" betas Conrad described aren't in the response.
- **SDK method signatures don't declare `as_of` in a discoverable way for older installs** — `as_of` is a keyword arg that only exists in 0.3.11+. A repo pinned to an older vendored copy silently lacks it (see next).
- **Vendored `sdk/riskmodels/` in RiskModels_API is still 0.3.10** and shadows the pip-installed 0.3.11 whenever `sdk/` is on `sys.path` (e.g. pytest). Upgrading the PyPI package does NOT update the repo's tests — the vendored copy needs a cross-repo sync to 0.3.11.
- **Confidential-treatment holdings** appear in historical vintages as rows with `ticker: null` and `security_id: "BW-RESTRICTED"` (e.g. Berkshire 2020-Q1, ~0.29% weight). Consumers must handle null tickers / possible duplicate `BW-RESTRICTED` rows.
- **`get_fundamentals` returns raw SEC facts, not the derived ratios Conrad described.** Rows carry `revenue`, `net_income`, `operating_income`, `total_assets`, `eps_diluted`, etc. — no precomputed ROE, revenue growth, or free cash flow; those must be derived client-side.
- **`get_fundamentals` coverage looks stale:** as-of-today (2026-07-13) the latest AAPL period is `period_end 2024-06-30` (filed 2024-08-02) — roughly a 2-year lag with `periods=8`. Point-in-time behavior itself is correct (as_of=2020-06-30 → latest filed 2019-07-31, not restated).
- **`get_etf_returns` is deprecated** in favor of `get_ticker_returns` (which now serves ETFs from `ds_etf.zarr`) — calling it emits a DeprecationWarning.
- **Returns coverage is solid for the multi-window backtest:** all 22 Berkshire positions + 23 overlay ETFs returned a full 2 years of daily gross returns via get_ticker_returns (499 trading days, 2024-07-15→2026-07-10), zero missing symbols. No returns gaps affected the chart.
