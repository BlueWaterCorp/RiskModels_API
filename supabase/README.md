# Supabase (RiskModels_API)

- **Migrations:** `migrations/` is **gitignored** on the public repo. See [docs/SUPABASE_MIGRATIONS.md](../docs/SUPABASE_MIGRATIONS.md).
- **Local temp:** `supabase/.temp/` is gitignored (CLI metadata).

Maintainers keep SQL under `supabase/migrations/` locally and sync the canonical copy to **BWMACRO** `private/riskmodels-supabase-migrations/`.
